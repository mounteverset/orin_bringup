# Nav2 overrides for indoor navigation with a SLAM Toolbox occupancy map.
global_costmap:
  global_costmap:
    ros__parameters:
      rolling_window: false
      resolution: 0.05
      track_unknown_space: true
      static_layer:
        enabled: true

